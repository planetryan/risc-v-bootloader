[package]
name = "bootloader"
version = "0.1.0"
edition = "2021"

[dependencies]
# Ensure all dependencies here are compatible with `#![no_std]`

[build-dependencies]

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"
